async function loadArticle(){const root=document.querySelector("[data-article]");if(!root)return;const params=new URLSearchParams(window.location.search);const id=params.get("id");try{const response=await fetch("data/news.json");const news=await response.json();const article=news.find(item=>item.id===id)||news[0];document.title=`${article.title} — FriendlyAI`;root.innerHTML=`<div class="article-hero"><a class="back-link" href="index.html#news">← Все новости</a><div class="article-meta"><span>${article.category}</span><time datetime="${article.date}">${formatDate(article.date)}</time><span>${article.readTime}</span></div><h1>${article.title}</h1><p>${article.description}</p><img src="${article.image}" alt="${article.title}"></div><article class="article-content">${renderContent(article.content)}</article><section class="article-cta"><h2>Хотите внедрить похожее решение?</h2><p>Разберём ваш процесс, оценим точки автоматизации и предложим AI-сценарий с понятным ROI.</p><div class="article-actions"><a class="btn btn-primary" href="https://t.me/chilbilove" target="_blank" rel="noopener noreferrer">Обсудить в Telegram</a><a class="btn btn-secondary" href="mailto:Kpi1t@ya.ru">Написать на email</a></div></section>`;}catch(error){root.innerHTML=`<p class="news-error">Материал временно недоступен.</p>`;}}function renderContent(blocks){return blocks.map(block=>{if(block.type==="h2")return`<h2>${block.text}</h2>`;if(block.type==="p")return`<p>${block.text}</p>`;if(block.type==="ul")return`<ul>${block.items.map(item=>`<li>${item}</li>`).join("")}</ul>`;return"";}).join("");}function formatDate(dateString){return new Intl.DateTimeFormat("ru-RU",{day:"numeric",month:"long",year:"numeric"}).format(new Date(dateString));}loadArticle();